a = {
  "value": "shot",
}

maps = {
  "a": "a",
  "b": "b",
  "c": _me.a + _me.b,
  "d": [
    _me._p.a,
    _me._p.b,
  ]
}

maps.hello = "hello"

maps.set(k > "hello" => "new" + v)

maps.replace(k > "hello" => {"new" + k: "new" + v})

maps.filter(k > "hello")

maps.del(k > "hello")

maps.keys(k == "hello")

maps.values(k == "hello")

maps.merge({"hello_1": "hello_1"})

maps + {
    "hello_1": "hello_1"
}

arr.set(i > 3 => v + " v > 3")

arr.set(i > 3 => v + " v > 3")
