a = {
  "value": "shot",
}

maps = {
  "a": "a",
  "b": "b",
}

maps SET <k,v>(k == "hello") {
  k: "c"
}, (k > "hello") {
  k: "d"
}

maps DEL <k,v>(k == "hello")

maps GET <k,v>(k != "hello")

maps CON {"hello": "world"}

{
  "hello": (self.world),
  "world": maps CON {"hello": "world"}
}
